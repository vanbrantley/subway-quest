# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.Python
build/
dist/

# Virtual environments
venv/
.venv/
env/
ENV/

# Jupyter
.ipynb_checkpoints/

# --- macOS ---
.DS_Store

# --- Editor ---
.vscode/
.idea/

# --- Expo / React Native (for when mobile/ is scaffolded) ---
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/*.jks
mobile/*.p8
mobile/*.p12
mobile/*.key
mobile/*.mobileprovision
mobile/.env
mobile/.env.local

# Copied pipeline output — regenerated via `node scripts/sync-data.js`, not hand-edited
mobile/src/data/*.json

# Metro
.metro-health-check*

# --- Env / secrets ---
.env
.env.local
*.pem

# --- Data (large raw/source files) ---
# Raw MTA/GTFS source files are regenerable from the public feed — not tracked.
# See network/raw/README.md for where to download them.
network/raw/*
!network/raw/README.md

# network/processed/ IS tracked — it's the actual pipeline output the app depends on.